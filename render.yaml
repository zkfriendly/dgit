services:
  - type: web
    name: dgit-web
    runtime: static
    rootDir: web
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 24
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
